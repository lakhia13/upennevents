# Base URL of the PostgREST API (see ../docker-compose.yml "postgrest" service).
# Local dev default matches the port PostgREST is published on in docker-compose.yml.
VITE_POSTGREST_URL=http://localhost:3001
